# 핸드오프 — AI 답변, 믿어도 될까? (fastfashion-ai-verify)

작성일: 2026-09-19 · 대상 저장소: `yerang-k/fastfashion-ai-verify` (main 최신 = PR #8 머지 시점)

## 1. 앱 개요
고2 주제탐구독서 수업용 웹앱. 학생이 AI 답변 속 문장 5개를 모둠별로 검증(출처·정확성·신뢰성 → 4단계 판정)하고, 교사가 화면으로 수업 진행을 제어한다.

| 구분 | 주소/위치 |
|---|---|
| 학생 화면 | https://yerang-k.github.io/fastfashion-ai-verify/ (반별: `?class=1-3`) |
| 교사 화면 | …/teacher.html (반별 `?class=…`, 프로젝터 무대 화면 `?view=stage`) |
| 서버 | Apps Script 웹앱, 주소는 `config.js`의 `API_URL` (`/exec`) |
| 데이터 | Apps Script가 묶인 구글 스프레드시트 (학생응답·모둠기록지·명단·설정 시트, 반별은 `_반이름` 접미사) |
| 교사 PIN | Apps Script 프로젝트 설정 → 스크립트 속성 `TEACHER_PIN` (10회 실패 시 10분 잠금, 모든 반 공통) |

파일: `index.html`(학생), `teacher.html`(교사), `config.js`(API_URL·CLASS_ID), `content.js`(기본 주장·자료), `lesson.js`(수업 구조 기본값·정규화·`STAGE_GUIDE`), `gas/Code.gs`(서버).

## 2. 이번에 한 일 (PR #1~#8)
1. **#1 개편**: 교사가 `lesson.current`(현재 단계 id)를 지정하면 학생은 그 단계 화면만 봄(RE:SEARCH 방식). `null`=자유 이동. 반별 분리(`?class=`), 무대 화면, 모노톤+베이지(`#c9b18b`) 디자인.
2. **#2·#4·#5 참여 코드 날짜 변환 버그**: 구글 시트가 `2-3-07`을 날짜로 바꿈 → 코드 칸을 텍스트 서식으로 지정 후 기록(`putRow_`), 옛 날짜 행은 학생에게 안 내려주고 교사 화면에서 삭제 가능.
3. **#3 교사 화면 단순화**: 탭 아이콘(📊현황/📝결과 정리/⚙️수업 설정/📁데이터), 참관 자료 탭 삭제, 학생별 코드·모둠 수정은 `수업 설정 › ⑤ 학생 관리`로 이동, 수업 설정 5개 섹션 접기/펴기(펼침 상태 유지).
4. **#6 학생 화면 빈 화면 버그**: 한 줄 주석이 다음 줄 `let cur=…`를 삼킴 → 수정.
5. **#7 무대 화면 개선**: 지금 할 일 문장·진행 방법 카드·단계별 패널(판정 4단계 / 모둠별 문장·판정 / 발표·판정 공개 / 제출 현황)·타이머 막대.
6. **#8 속도 개선**: 읽기 요청 잠금 제거, 수업 설정·명단 CacheService 캐시, 학생 모둠기록지 조회는 3~5단계만, 교사 단계 버튼 즉시 반영(낙관적 갱신).

## 3. 배포 절차 (중요)
- **Code.gs는 자동 배포되지 않는다.** clasp 설정이 없어 Apps Script 에디터에 붙여 넣고 **"배포 관리 → 수정 → 새 버전"**으로 재배포해야 한다. "새 배포"를 만들면 `/exec` 주소가 바뀌어 기존 링크가 죽는다.
- **순서: Code.gs 재배포 → GitHub 푸시.** 프런트만 먼저 올리면 서버가 새 명령(`setStage` 등)을 몰라 학생이 준비 단계에 갇힐 수 있다.
- 화면(HTML/JS)만 바뀐 변경은 재배포 불필요. GitHub Pages는 푸시 후 1~2분 뒤 반영(브라우저 캐시는 Ctrl+F5).
- Git 흐름: 새 브랜치(최신 main에서) → 커밋 → GitHub REST API로 PR 생성·스쿼시 머지·브랜치 삭제(gh CLI 없음, git credential의 `yerang-k` 토큰 사용).

## 4. 남은 일 / 확인 필요
- [ ] **PR #8(속도 개선)의 Code.gs 재배포 여부 미확인.** 재배포 후 `curl`로 동시 요청 시간을 다시 재 볼 것(개선 전: 단건 2~2.8초, 동시 10건 최대 7초).
- [ ] 구글 시트의 테스트 탭 **`학생응답_zztest`(+ `zztest` 반) 삭제** — 삭제 전까지 교사 화면 반 목록에 "zztest"가 보임.
- [ ] 예전에 날짜로 바뀐 코드 행(시트 **명단**·**학생응답** 기본 반)이 남아 있으면 삭제. 교사 화면 `수업 설정 › ⑤ 학생 관리`에서 `2002-03-07T…` 형태 행 삭제 가능.
- [ ] 무대 화면·교사 화면은 **가짜 서버로만** 화면 검증함. 실제 수업 데이터로 한 번 훑어볼 것.
- 서버 응답 1회 1~2초는 Apps Script 특성(줄일 수 없음). 교사가 단계를 넘기면 학생 화면 반영까지 최대 약 4초(학생 폴링 주기 4초 + 응답 시간). 필요하면 `index.html`의 `setInterval(…,4000)` 조정.

## 5. 알려진 함정
- **구글 시트는 `2-3-07` 같은 문자열을 날짜로 자동 변환한다.** 사용자 입력 문자열을 시트에 쓸 때는 반드시 텍스트 서식 지정 후 쓸 것. `appendRow`는 열 서식을 무시하므로 쓰지 말 것(`putRow_` 사용).
- 화면 스크립트를 고친 뒤에는 문법 검사(`node --check`)만 하지 말고 **실제로 열어 렌더링까지 확인**.
- 학생 화면은 '준비 완료'를 누르지 않은 학생을 준비 단계에 붙잡아 둔다(교사 명령이 와도 이동 안 함, 의도된 동작).
- 학생 정보(코드·응답)는 구글 워크스페이스(학교 공식 인프라) 안에만 저장한다는 사용자 원칙 유지 — 제3자 서버 사용 금지.
- 테스트 시 브라우저 미리보기 탭이 백그라운드(`document.hidden`)면 화면이 갱신되지 않고 학생 폴링도 멈춘다.

## 6. 구조 메모
- 학생 서버 API(`gas/Code.gs`): 읽기 `getLesson/getStudent/getGroup/getShare/teacherAll`(잠금 없음), 쓰기 `saveStudent/saveGroup/setLesson/setStage/setRoster/updateStudent/deleteStudent/help/helpClear/setShare`(스크립트 잠금).
- 요청마다 `class` 파라미터로 반 구분(`sn_()`=시트 이름, `ck_()`=설정 키 접미사). 기본 반은 접미사 없음.
- `lesson`(설정 시트의 JSON)에 단계·모둠·주장·`current`·`rosterOnly` 저장. `setLesson`은 `current`를 건드리지 않고, 단계는 `setStage`로만 바뀜.
- 단계별 안내 문구(무대 화면)는 `lesson.js`의 `STAGE_GUIDE`에서 수정.
