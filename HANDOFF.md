# 핸드오프 — AI 답변, 믿어도 될까? (fastfashion-ai-verify)

작성일: 2026-09-19 (2026-09-20 갱신) · 대상 저장소: `yerang-k/fastfashion-ai-verify` (main 최신 = PR #9 + 모둠 인증·코드 가림 PR 머지 시점)

## 1. 앱 개요
고2 주제탐구독서 수업용 웹앱. 학생이 AI 답변 속 문장 5개를 모둠별로 검증(출처·정확성·신뢰성 → 4단계 판정)하고, 교사가 화면으로 수업 진행을 제어한다.

| 구분 | 주소/위치 |
|---|---|
| 학생 화면 | https://yerang-k.github.io/fastfashion-ai-verify/ (반별: `?class=1-3`) |
| 교사 화면 | …/teacher.html (반별 `?class=…`, 프로젝터 무대 화면 `?view=stage`) |
| 서버 | Apps Script 웹앱, 주소는 `config.js`의 `API_URL` (`/exec`) |
| 데이터 | Apps Script가 묶인 구글 스프레드시트 (학생응답·모둠기록지·명단·설정 시트, 반별은 `_반이름` 접미사) |
| 교사 PIN | Apps Script 프로젝트 설정 → 스크립트 속성 `TEACHER_PIN` (10회 실패 시 10분 잠금, 모든 반 공통) |

파일: `index.html`(학생), `teacher.html`(교사), `config.js`(API_URL·CLASS_ID), `content.js`(기본 주장·자료), `lesson.js`(수업 구조 기본값·정규화·`STAGE_GUIDE`), `gas/Code.gs`(서버).

## 2. 이번에 한 일 (PR #1~#8)
1. **#1 개편**: 교사가 `lesson.current`(현재 단계 id)를 지정하면 학생은 그 단계 화면만 봄(RE:SEARCH 방식). `null`=자유 이동. 반별 분리(`?class=`), 무대 화면, 모노톤+베이지(`#c9b18b`) 디자인.
2. **#2·#4·#5 참여 코드 날짜 변환 버그**: 구글 시트가 `2-3-07`을 날짜로 바꿈 → 코드 칸을 텍스트 서식으로 지정 후 기록(`putRow_`), 옛 날짜 행은 학생에게 안 내려주고 교사 화면에서 삭제 가능.
3. **#3 교사 화면 단순화**: 탭 아이콘(📊현황/📝결과 정리/⚙️수업 설정/📁데이터), 참관 자료 탭 삭제, 학생별 코드·모둠 수정은 `수업 설정 › ⑤ 학생 관리`로 이동, 수업 설정 5개 섹션 접기/펴기(펼침 상태 유지).
4. **#6 학생 화면 빈 화면 버그**: 한 줄 주석이 다음 줄 `let cur=…`를 삼킴 → 수정.
5. **#7 무대 화면 개선**: 지금 할 일 문장·진행 방법 카드·단계별 패널(판정 4단계 / 모둠별 문장·판정 / 발표·판정 공개 / 제출 현황)·타이머 막대.
6. **#8 속도 개선**: 읽기 요청 잠금 제거, 수업 설정·명단 CacheService 캐시, 학생 모둠기록지 조회는 3~5단계만, 교사 단계 버튼 즉시 반영(낙관적 갱신).

7. **#9 공개수업 대비**(2026-09-20): ① 수업 시계 — '수업 시작'부터 흐르는 전체 시간(서버 `설정` 시트 `clock` 키에 시작 시각 저장, 단계와 무관). 옛 단계별 카운트다운·2/5/10분 버튼 삭제 ② 모둠 기록지 칸에 학생이 직접 쓴 값을 `data._mine`에 따로 보관(결과 정리 '모둠원별 작성 내용'·학생 CSV에 표시) ③ PIN 실패를 기기(`cid`)별로 집계, 전체 실패 100회/10분 초과 시 로그인한 기기(6시간)만 허용 ④ '명단만 입장'을 서버(`saveStudent_`)에서도 강제 ⑤ 단계 고정 중 필수 항목이 비어도 제출 허용 ⑥ 폴링 겹침 방지(`pollTick`) ⑦ Esc로 무대 화면이 닫히지 않음(전체화면 해제 시에만) ⑧ 학생 쪽 요청은 교사가 만든 반에만 허용
8. **모둠 인증·코드 가림**(2026-09-20): `saveGroup/getGroup/help`는 코드+기기번호(`dev`)가 저장된 학생 행과 맞을 때만 그 학생의 모둠으로 동작(`studentGroup_`). `getStudent`는 `dev`를 내려주지 않고 `sameDev`만 반환. 교사 화면은 학생 코드를 기본으로 흐리게 표시(헤더 '🙈 코드 가림' 버튼, 마우스를 올리면 보임)
9. **모둠 탐구를 두 활동으로 분리**(2026-09-20): 단계 id 3='개인 검증'(각자 검증 기록지·판정·근거·보고서용 문장. 칸 id `i_*`, 각자의 응답에만 저장·모둠에 안 보임), 새 단계 id 7='모둠 정리'(모둠이 발표할 판정·근거·고쳐 쓴 문장 `g_verdict/g_reason/g_rewrite/g_speaker`). 기본 단계 순서는 0,1,2,3,7,4,5,6(`normLesson`이 저장된 옛 설정에도 7을 3 뒤에 끼워 넣음). **모둠 정리는 기록자 한 명만 입력**: 모둠 기록의 `_rec`에 기록자 코드를 저장, 기록자가 정해지기 전에는 아무도·정해진 뒤에는 기록자만 쓸 수 있고 서버(`saveGroup_`의 `locked`)도 다른 사람의 쓰기를 무시. 교사는 현황 표에서 기록자를 '해제'(`clearRecorder`)할 수 있음. 모둠 공유(단계 4)에는 모둠 정리의 발표 내용만 올라감. 제출 필수 항목은 개인 판정·개인 고쳐 쓴 문장. `setStage` 허용 id는 0~7. **저장된 수업 설정이 있는 반은 '⚙️수업 설정 ① 단계 구성'에서 3번 단계 이름·시간을 '개인 검증 9분'으로 고쳐야 총 50분이 맞음**(옛 설정의 '모둠 탐구 17분'이 남아 있으면 합계 58분)

10. **저장하기 버튼**(2026-09-20, 이후 확대: 생각 열기(1)·개인 검증(3)·모둠 공유(4)·정리(5)의 개인 응답 전부 + 모둠 정리(7)): 학생 응답은 **단계별로 따로** 저장된다(각 단계의 저장하기는 그 단계에서 쓴 값만 `data._snap`에 스냅샷해 서버로 보냄, 저장 전 입력은 서버에 안 감. 저장 전 표시는 `data._pend[단계]`/`data._unsent[단계]`). 입력할 때마다 서버로 보내지 않고, 이 기기에만 임시 저장한 뒤 '💾 저장하기'를 눌러야 서버에 저장(개인=`pushStudent`, 모둠=`pushGroup`, 기록자만 가능). 저장 전 표시는 `data._ipend`(개인)·`data._gpend`/`gdirty`(모둠, 새로고침해도 복원). 저장하기를 안 누르고 단계를 떠나면(`autoSaveLeaving`)·제출하면 자동 저장. 화면만 바뀌었고 Code.gs 재배포는 불필요
11. **무대 화면 한 화면 맞춤·상단 고정**(2026-09-20): 무대 화면(`teacher.html`의 `#stageView`)은 스크롤 없이 화면(100vh)에 딱 맞음. **맨 위 '단계 이름'과 '시계'는 같은 크기(최대 72px)로 고정**되어 단계를 넘겨도 위치가 변하지 않음. 그 아래 내용(`.sv-body`)은 남은 공간에 맞춰 `fitStage()`가 `--k`(0.5~2.2배)로 자동 조절: 짧은 단계는 키우고(패널이 없으면 진행 방법 카드가 높이를 채움), 긴 단계(모둠이 많거나 문장이 길 때)는 줄이며, 그래도 넘치면 카드 문장을 1줄로. 내용 영역 크기 단위는 컨테이너 쿼리 `cqh` '지금 할 일' 문장(`.sv-todo`)은 항상 **한 줄**: `fitTodo()`가 자간을 먼저 좁히고(최대 -0.05em) 그래도 넘치면 글자 크기를 줄임. 세부 단계 카드(`.sv-points`)는 내용 높이대로(예전 모양), 글자는 어절 단위로 줄바꿈(`word-break:keep-all`+`text-wrap:balance`+`wj()`가 어절 안에 단어 결합자 U+2060을 넣어 `·`·`%` 등에서 끊기지 않게). 모둠 카드 문장은 가능하면 전부 보이게 하고(`data-clamp` 0→3→2→1줄 순으로 제한) 준비(0)·생각 열기(1)·마무리(6) 단계는 `.sv-inner.big` 모드: 진행 방법 카드를 세로로 쌓고 글자를 크게(다른 단계는 영향 없음). **모둠 공유(4) 발표 강조**: 무대 화면에서 모둠 카드를 누르면(또는 숫자 키 1~9·0=10모둠) 그 모둠의 발표 내용(담당 문장·판정·근거 한 문장·보고서용 문장)이 크게 보이고(`SPOT`/`spot(n)`), 강조 화면을 다시 누르면 작아짐. 아래 칩으로 다른 모둠으로 전환, 단계를 넘기면 해제. 판정 공개 전에도 발표 화면에는 내용이 보임. 내용이 길면 글자를 줄이다가(최소 1배) 그래도 길면 강조 화면 안에서 스크롤(칩은 항상 아래 고정). **무대 화면 아래 단계 버튼(`.sv-steps span`)을 누르면 바로 그 단계로 이동**(`setStage(id)`, 이전·다음 버튼도 그대로)

## 3. 배포 절차 (중요)
- **Code.gs는 자동 배포되지 않는다.** clasp 설정이 없어 Apps Script 에디터에 붙여 넣고 **"배포 관리 → 수정 → 새 버전"**으로 재배포해야 한다. "새 배포"를 만들면 `/exec` 주소가 바뀌어 기존 링크가 죽는다.
- **순서: Code.gs 재배포 → GitHub 푸시.** 프런트만 먼저 올리면 서버가 새 명령(`setStage` 등)을 몰라 학생이 준비 단계에 갇힐 수 있다.
- 화면(HTML/JS)만 바뀐 변경은 재배포 불필요. GitHub Pages는 푸시 후 1~2분 뒤 반영(브라우저 캐시는 Ctrl+F5).
- Git 흐름: 새 브랜치(최신 main에서) → 커밋 → GitHub REST API로 PR 생성·스쿼시 머지·브랜치 삭제(gh CLI 없음, git credential의 `yerang-k` 토큰 사용).

## 4. 남은 일 / 확인 필요
- [ ] **Code.gs 재배포 필요**(PR #9 이후 변경분: 시계·PIN 기기별 잠금·명단 서버 차단·모둠 인증). 재배포 전에는 '수업 시작' 버튼이 실패 안내를 띄움. 재배포 후 교사 화면·무대 화면 기기를 모두 한 번씩 로그인해 둘 것(PIN 전체 실패 폭주 시 로그인한 기기만 통과)
- [ ] 구글 시트의 테스트 탭 **`학생응답_zztest`(+ `zztest` 반) 삭제** — 삭제 전까지 교사 화면 반 목록에 "zztest"가 보임.
- [ ] 예전에 날짜로 바뀐 코드 행(시트 **명단**·**학생응답** 기본 반)이 남아 있으면 삭제. 교사 화면 `수업 설정 › ⑤ 학생 관리`에서 `2002-03-07T…` 형태 행 삭제 가능.
- [ ] 무대 화면·교사 화면은 **가짜 서버로만** 화면 검증함. 실제 수업 데이터로 한 번 훑어볼 것.
- 서버 응답 1회 1~2초는 Apps Script 특성(줄일 수 없음). 교사가 단계를 넘기면 학생 화면 반영까지 최대 약 4초(학생 폴링 주기 4초 + 응답 시간). 필요하면 `index.html`의 `setInterval(…,4000)` 조정.

## 5. 알려진 함정
- **구글 시트는 `2-3-07` 같은 문자열을 날짜로 자동 변환한다.** 사용자 입력 문자열을 시트에 쓸 때는 반드시 텍스트 서식 지정 후 쓸 것. `appendRow`는 열 서식을 무시하므로 쓰지 말 것(`putRow_` 사용).
- 화면 스크립트를 고친 뒤에는 문법 검사(`node --check`)만 하지 말고 **실제로 열어 렌더링까지 확인**.
- 학생 화면은 '준비 완료'를 누르지 않은 학생을 준비 단계에 붙잡아 둔다(교사 명령이 와도 이동 안 함, 의도된 동작).
- 학생 정보(코드·응답)는 구글 워크스페이스(학교 공식 인프라) 안에만 저장한다는 사용자 원칙 유지 — 제3자 서버 사용 금지.
- 테스트 시 브라우저 미리보기 탭이 백그라운드(`document.hidden`)면 화면이 갱신되지 않고 학생 폴링도 멈춘다.

## 6. 구조 메모
- 학생 서버 API(`gas/Code.gs`): 읽기 `getLesson/getStudent/getGroup/getShare/teacherAll`(잠금 없음), 쓰기 `saveStudent/saveGroup/setLesson/setStage/setRoster/updateStudent/deleteStudent/help/helpClear/setShare`(스크립트 잠금).
- 요청마다 `class` 파라미터로 반 구분(`sn_()`=시트 이름, `ck_()`=설정 키 접미사). 기본 반은 접미사 없음.
- `lesson`(설정 시트의 JSON)에 단계·모둠·주장·`current`·`rosterOnly` 저장. `setLesson`은 `current`를 건드리지 않고, 단계는 `setStage`로만 바뀜.
- 단계별 안내 문구(무대 화면)는 `lesson.js`의 `STAGE_GUIDE`에서 수정.
